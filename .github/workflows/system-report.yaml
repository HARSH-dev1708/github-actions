name: System Report

on:
  push:

jobs:
  system-info:
    runs-on: ubuntu-latest

    steps:
        - name: Print OS and Kernal Information
          run: |
            echo "Operating System: $(uname -s)"
            echo "Kernel Version: $(uname -r)"
            

  tool-check:
    runs-on: ubuntu-latest